# v5 릴리즈 PRD — 최종보고서 제출 (Final Report Submission)

> 성격: v5 화면은 이미 그려져 있으나 노란 박스 PRD가 없어, **기존 화면을 근거로 역방향 복원한 PRD**다.
> 근거 자료: `design/operations/prd-pdf/img/v5-overview.jpg`, `v5-s1.jpg`, `v5-s2.jpg`, `v5-s5.jpg` (렌더 이미지 정독).
> v5.txt는 폰트 매핑이 깨져 신뢰하지 않고, 이미지에서 읽어낸 사실만 사용했다.
> 값 표기: 화면에서 직접 확인한 것은 근거 그대로, 관례로 채운 것은 **[기본값]** 표시.
> 화면 ID 체계: 일반 사용자 지원 흐름 = A 시리즈 (SITEMAP 규칙). v5는 A 시리즈의 최종보고서 하위 흐름이다.

---

## 릴리즈 개요

- **릴리즈**: v5 — 최종보고서 제출
- **한 줄 정의**: 본선 진출 팀이 최종 기획·개발 보고서를 8개 섹션 단계별 폼으로 작성하고, 마지막 9번째 단계에서 발표자료·데모·소스코드를 묶어 하나의 패키지로 최종 제출하는 흐름.
- **소속 흐름**: 마이페이지 "내 지원 현황" → 각 지원서의 [이어 작성하기] → 최종보고서 스텝 폼.
- **선행 릴리즈 의존**: v3 지원 흐름(A-01/A-02)·상태 머신, v4 1차 합격 발표(본선 진출 판정). 본선 진출 팀만 이 흐름에 진입한다.

---

## 화면 목록

| ID | 이름 | 역할 |
| --- | --- | --- |
| A-30 | 내 지원 현황 (최종보고서 진입점) | 마이페이지 탭. 지원서별 최종보고서 작성 진행률·상태 표시, 이어 작성 진입 |
| A-31 | 최종보고서 스텝 폼 | 좌측 9단계 스텝 네비 + 우측 섹션 폼. SECTION 1/8~8/8이 콘텐츠 작성 단계 |
| A-32 | 최종 심사 제출 패키지 | SECTION 9/9. 4종 산출물(보고서/발표자료/데모URL/소스코드) 통합 제출 |

> 스텝 넘버링 근거: 좌측 네비는 9개 항목이나 콘텐츠 섹션 헤더는 "SECTION 1/8"로 표기되고, 마지막 패키지 단계만 "SECTION 9/9"로 표기된다(화면 확인). 즉 1~8은 작성 섹션, 9는 제출 패키지 단계.

---

## A-30 · 내 지원 현황 (최종보고서 진입점)

### 1. 화면 개요
- 마이페이지 좌측 네비: 내 정보 / **내 지원 현황(활성)** / 내 팀 / 팀빌딩 지원 현황. 사용자 카드 상단(이름 "최보람" + 로그아웃).
- 우측: "내 지원 현황" 제목 + 상단 안내 배너 + 지원서 카드 리스트.

### 2. 진입 경로 + 조건
- GNB 마이페이지 → 내 지원 현황 탭.
- 본선 진출 지원서가 있으면 최종보고서 작성 카드가 노출된다.

### 3. 데이터 로딩 정책 + 예외
- 로딩=스켈레톤, 에러="데이터를 불러오는 데 실패했습니다" + [다시 시도], 빈상태=문구 + CTA. **[기본값]** (공통 규칙)

### 4. 상태 머신 (지원서 카드 상태)

최종보고서 제출은 **명시적 완결 이벤트**다(A-32의 [최종 패키지 일괄 제출하기]). 따라서 지원서 카드는 아래 상태를 오간다.

- **본선 진행중 · 작성중** (Red 계열 배지) — 아직 최종 제출 전. 작성 진행률 표시(예: `1/8`).
- **제출완료** (Green 배지) — 최종 패키지를 제출한 상태. 제출일시 표시.
- **재제출 필요** (Red 배지) — 제출 후 항목을 수정했으나 아직 재제출하지 않은 상태. 단일 배지로 유저에게 "지금 해야 할 일"을 명확히 전달한다.
- **본선 미진출** (Gray 배지) — 본선 탈락. 제출일시만 표시, 작성 진입 없음.

**전이 트리거:**
| 전이 | 트리거 |
| --- | --- |
| (없음) → 본선 진행중·작성중 | 1차 합격 발표(v4, 관리자 액션) **[기본값]** |
| 본선 진행중·작성중 → 제출완료 | A-32에서 [최종 패키지 일괄 제출하기] 성공 |
| 제출완료 → 재제출 필요 | 제출 후 임의 항목(폼 섹션/발표자료/URL/소스코드) 수정 |
| 재제출 필요 → 제출완료 | A-32에서 [변경사항 다시 제출하기] 성공 (제출일시 갱신) |
| 모든 편집 가능 상태 → 잠금 | 접수 마감(10/16 24:00) 도달 → 마지막 제출본이 최종본 |

> 자동 반영 아님: 제출 후 수정한 내용은 **재제출해야 심사 반영**된다. 수정만 하고 재제출하지 않으면 "수정사항 미반영" 상태로 남는다.

### 5. 상태별 UI 매트릭스
| 상태 | 배지 | 부가 표시 | CTA |
| --- | --- | --- | --- |
| 본선 진행중·작성중 | Red "본선 진행중" | 팀명 + "최종 수정 N시간 전" + "최종 보고서 작성 진행률 `1/8`" 프로그레스 | [이어 작성하기 →] |
| 제출완료 | Green "제출완료" | 팀명 + "제출일시 YY.MM.DD HH:MM" | [제출 내용 보기 →] |
| 재제출 필요 | Red "재제출 필요" | 팀명 + "최종 제출 YY.MM.DD HH:MM · 수정 후 미반영" | [수정사항 제출하기 →] |
| 본선 미진출 | Gray "본선 미진출" | 팀명 + "제출일시 26.05.12 23:11:56" | [제출 지원서 보기 →] |

> 배지 라벨은 잠정값이다. "수정사항 미반영" 문구·색은 검토 후 확정(넘버링 재검토와 함께).

### 6. 정렬·버튼 규칙
- 카드 우상단 [⋯] 더보기 메뉴. 카드 우하단 진입 CTA(텍스트 링크형 →).
- 정렬: 진행 중인 지원서 우선, 그다음 종료 지원서. **[기본값]**

### 7. 표기 규칙
- 상대시간: "N시간 전" 형식, 7일↑는 `YYYY.MM.DD`. **[기본값]** (공통 규칙)
- 제출일시 절대표기: `YY.MM.DD HH:MM:SS` (화면 확인: 26.05.12 23:11:56).
- 진행률: `현재단계/전체단계` 배지(`1/8`).

### 8. 레이아웃·인터랙션
- 상단 안내 배너(연노랑, 시계 아이콘): "10월 16일까지 제출을 완료해주세요. 제출완료 이후에도 접수 마감 전까지는 수정 가능합니다."
- 카드형 리스트, 카드 간 세로 스택.

---

## A-31 · 최종보고서 스텝 폼

### 1. 화면 개요
- 좌측: "지원서 항목" 스텝 네비(9개) + "8개 항목을 빠짐없이 작성해 주세요." 안내.
- 우측: 현재 섹션 폼. 헤더 "SECTION n / 8" + 섹션명.
- 예시로 확인된 SECTION 1/8 = 기본 정보(팀명 + 팀원 정보 테이블).

### 2. 진입 경로 + 조건
- A-30에서 [이어 작성하기] → 마지막 작성 위치의 섹션으로 진입.
- 상단 GNB에서 지원하기 컨텍스트 유지. 좌상단 [← 내 지원 현황]으로 복귀.

### 3. 데이터 로딩 정책 + 예외
- 각 섹션 진입 시 기존 입력값 프리필. 저장 실패 시 에러 문구 + 재시도. **[기본값]**

### 4. 상태 머신 (섹션별 작성 상태)
- 스텝 아이콘 3종(화면 확인):
  - **완료** — 보라 원형 체크(✔)
  - **미완료/현재** — 회색 원형 번호(예: 5번 "신규 메뉴!!"가 회색 번호)
  - **패키지 단계(9)** — 회색 번호 + "4종 통합" Red 배지, 하위 4항목 상태 리스트
- 전이: 필수 입력 충족 시 완료(✔)로 전환. **[기본값]**

### 5. 상태별 UI 매트릭스 (스텝 네비 항목)
| 스텝 상태 | 아이콘 | 표시 |
| --- | --- | --- |
| 완료 | 보라 ✔ | 섹션명 |
| 미완료/현재 | 회색 번호 | 섹션명 |
| 9. 최종 심사 제출 패키지 | 회색 9 + "4종 통합"(Red) | 하위: 1.최종 보고서(미완료) / 2.발표 자료(미등록) / 3.서비스 링크(미등록) / 4.소스코드 링크(미등록) — 각 우측 Gray 상태 라벨 |

- 확인된 9개 스텝: 1 기본 정보 / 2 서비스 정의 / 3 문제정의 / 4 AI서비스 구성계획 / 5 신규 메뉴!! / 6 활용 Data 및 연동시스템 / 7 확대전개 가능성 / 8 기대효과 / 9 최종 심사 제출 패키지.

### 6. 정렬·버튼 규칙
- 폼 우하단 [다음 →] 버튼(Red)으로 다음 섹션 이동.
- 스텝 네비 클릭으로 임의 섹션 점프. **[기본값]**

### 7. 표기 규칙
- 섹션 헤더 "SECTION n / 8"(Red small caps).
- 팀원 테이블 컬럼: 구분 / 이름 / 직급 / 본부 / 팀 / 이메일 / 역할.
- 구분값: 리더(Red 강조) / 팀원1 / 팀원2. 역할 배지: 기획 / 개발.

### 8. 레이아웃·인터랙션
- 2단 레이아웃(좌 스텝 네비 고정 폭 + 우 폼 영역).
- 기본 정보 섹션: 팀명(단일 입력) + 팀원 정보(ⓘ 툴팁) 테이블.

> **스코프 노트 (디자인 범위 제외):**
> - SECTION 2~8 각 섹션의 **내부 폼 필드 구성은 디자인에서 스킵**한다. 스텝 네비의 섹션명·상태만 다루고, 개별 필드 레이아웃은 이 PRD/화면 범위 밖이다.
> - 항목별 **필수/선택 필드 규칙도 스킵**하며, **별도 전달**한다. "8개 항목 빠짐없이 작성" 안내 문구만 화면에 유지.

---

## A-32 · 최종 심사 제출 패키지 (SECTION 9/9)

### 1. 화면 개요
- 헤더 "SECTION 9 / 9" + "최종 심사 제출 패키지".
- 설명: "작성하신 최종 기획 개발 보고서와 함께 심사에 필요한 발표 자료(PDF), 데모 링크, 소스코드를 하나의 패키지로 묶어 최종 제출합니다. 심사위원에게 한 화면에서 통합 제공되는 핵심 산출물입니다."
- "최종 제출 패키지 4종 구성 현황" 섹션 + 4개 카드(2×2 그리드) + 마감 경고 배너 + 하단 액션바.

### 2. 진입 경로 + 조건
- 스텝 네비 9번 클릭 또는 8번 섹션에서 [다음 →].

### 3. 데이터 로딩 정책 + 예외
- 각 구성요소의 완료/미등록 상태를 실시간 반영. **[기본값]**

### 4. 상태 머신 (4종 구성요소 + 제출)
- 각 구성요소: **완료**(Green ✔ 완료 배지) / **미등록**(Gray 배지).
- 제출 게이트: 4종이 요건을 충족해야 최종 제출 활성. **[기본값]**
- **제출 후 수정 정책 (확정 — 명시적 재제출 모델)**: 최종 제출은 [최종 패키지 일괄 제출하기]라는 **명시적 완결 액션**으로만 이루어진다. 제출 후에도 접수 마감(10/16 24:00) 전까지 각 항목을 수정할 수 있으나, **수정분은 [변경사항 다시 제출하기]로 재제출해야 심사에 반영**된다(자동 반영 아님). 재제출 전까지는 "제출완료 · 수정사항 미반영" 상태로 남는다. 마감 시각 도달 시 전체 잠금되며, 마지막으로 제출된 버전이 최종본이다. (근거: 화면에 "일괄 제출" 액션이 별도 존재 → 제출은 완결 이벤트. 상태 라벨은 잠정, 검토 후 확정)

### 5. 상태별 UI 매트릭스 (4종 구성)
| # | 구성요소 | 설명 | 완료 시 | 미완료 시 |
| --- | --- | --- | --- | --- |
| 1 | 기획/개발 최종 보고서 | 기본정보·문제정의·아키텍처 등 총 8개 파트 | Green ✔ 완료. [최종 보고서 확인](Red) + [발표 자료 초안 생성 (.pptx)](Purple) | — |

**구성요소 1의 두 액션 동작 (확정):**
- **[최종 보고서 확인]** — SECTION 1~8에 입력한 내용을 **팝업 모달 뷰어**로 통합 제공한다. 별도 화면 이동 없이 현재 화면 위에 모달로 열려, 8개 섹션 작성분을 한 번에 검토한다. (읽기 전용 뷰어)
- **[발표 자료 초안 생성 (.pptx)]** — **AI가** SECTION 1~8 입력 내용을 근거로 발표자료 초안(.pptx)을 생성한다. 생성 결과는 구성요소 2(발표 자료) 등록 후보로 이어진다.
| 2 | 발표 자료(PDF) | 최종 심사용 발표 자료 파일 등록 | 파일 카드(예: `...v1.2.pptx` 18.4MB, [미리보기]) | Gray 미등록 + 업로드 드롭존 "최대 50MB · PDF 파일만 업로드 가능" |
| 3 | 서비스 데모 URL | 실제 동작 가능한 웹/앱 데모 주소 | Green ✔ 완료. URL(`https://atelier-ai-demo.lge.internal`) + [테스트 ↗] | — |
| 4 | 소스코드 저장소 | 사내 GitLab | Green ✔ 완료. URL(`https://github.com/lge-ax-hackathon/team-atelier`) + [저장소 ↗] | — |

### 6. 정렬·버튼 규칙
- 4종 카드 2×2 배치(1·2 상단, 3·4 하단).
- 하단 액션바(좌→우): [← 이전] · 안내문(수정=재제출 취지) · [임시 저장](outline) · [최종 패키지 일괄 제출하기 →](Red primary).

**하단 제출 버튼 상태값 (핵심):**
| 시점 | 버튼 라벨 | 스타일/활성 | 조건 |
| --- | --- | --- | --- |
| 최초 제출 전 (4종 미충족) | 최종 패키지 일괄 제출하기 → | Red primary, **비활성** | 4종 요건 미충족 시 클릭 불가 |
| 최초 제출 전 (4종 충족) | 최종 패키지 일괄 제출하기 → | Red primary, **활성** | 제출 가능 |
| 제출 완료 · 변경 없음 | 제출 완료됨 ✓ | 비활성(Green 또는 muted) + "N에 제출됨" 표기 | 재제출할 변경분 없음 |
| 제출 후 항목 수정함 | **변경사항 다시 제출하기 →** | Red primary, **재활성** | 미반영 수정분 존재 |
| 접수 마감 이후 | 제출 마감됨 | 비활성(잠금 아이콘) | 마감 시각 경과 |

- [임시 저장]은 제출과 무관하게 항시 사용 가능(마감 전). 임시 저장은 상태를 "제출완료"로 바꾸지 않는다.
- 규칙 요약: **제출은 명시적 액션으로만 완결**되고, 제출 후 수정하면 버튼이 [변경사항 다시 제출하기]로 재활성된다. 자동 반영 없음.

### 7. 표기 규칙
- 파일 크기: `18.4MB` 형식. 완료 배지=Green "✔ 완료", 미등록=Gray "미등록".
- 외부 링크는 [↗] 아이콘 동반.

### 8. 레이아웃·인터랙션
- 마감 경고 배너(연분홍/Red 계열): "심사 제출 패키지 유의사항 — 제출 마감은 10/16(목) 24:00 이후 마감됩니다. ... D-day 잔여 표시". (텍스트 일부는 이미지 해상도 한계로 축약)
- 카드별 인라인 액션(업로드/테스트/미리보기/저장소 열기).

---

## 릴리즈 공통 규칙 적용 (SITEMAP §5)

- 상태 뱃지 색: 완료/접수완료=Green · 진행중=Red 계열(본선 진행중) · 미제출/미등록=Gray.
- 상대 시간: 방금 전 / N분 전 / N시간 전 / N일 전 / 7일↑ `YYYY.MM.DD`.
- 상단 네비: 홈 / 지원하기 / 공지사항 / FAQ / 로그인·마이페이지(우측 Red CTA).
- 로딩/예외: 로딩=스켈레톤, 에러 문구 + [다시 시도], 빈상태=문구 + CTA.

---

## 의존관계 (변경 시 영향, SITEMAP §7)

- **제출 마감(10/16 24:00)** 변경 → A-30 배너, A-32 경고 배너·D-day, 제출 활성 조건 모두 영향.
- **본선 진출 판정(v4)** 변경 → A-30 카드 노출·상태(본선 진행중/미진출).
- **8개 섹션 구성** 변경 → A-31 스텝 네비, A-32 "4종 구성 현황"의 보고서 파트 수.
- **패키지 4종 요건** 변경 → A-32 제출 게이트, 각 구성요소 완료 판정.

---

## 확인 항목 처리 결과 (성능 평가 참고)

초안 복원 시 미확정이던 항목들을 사용자 확인으로 정리한 결과다.

| # | 항목 | 처리 | 결정 |
| --- | --- | --- | --- |
| 1 | SECTION 2~8 내부 폼 필드 구성 | **스킵** | 디자인 범위 밖. 스텝 네비 섹션명·상태만 다룸 (A-31 §8 스코프 노트) |
| 2 | 항목별 필수/선택 필드 규칙 | **스킵 · 별도 전달** | 화면엔 "8개 항목 빠짐없이 작성" 안내만 유지, 규칙은 별도 전달 |
| 3 | 제출 후 수정 정책 | **확정 (재제출 모델)** | 제출 후 수정 시 "재제출 필요" 단일 배지로 전환. 재제출해야 심사 반영. 마감 시 잠금 (A-30 §4, A-32 §4·§6) |
| 4 | 구성요소 1의 두 액션 | **확정** | [최종 보고서 확인]=SECTION 1~8 팝업 모달 뷰어 / [발표 자료 초안 생성]=AI 생성 (A-32 §5) |
| 5 | 화면 ID·스텝 넘버링 | **보류** | 전체 넘버링 체계 재검토 필요. A-30~32는 잠정값 |

> 성능 평가 관점 메모: 초안 단계에서 **구성요소 1의 [최종 보고서 확인] 버튼 동작을 확인 목록에 올리지 못한 것**이 실제 누락이었다.
> 바로 옆 [발표 자료 초안 생성]은 물었으면서 인접 버튼은 놓쳐 일관성이 없었다. 버튼의 "존재"는 읽었으나 "그 버튼이 무엇을 여는가"를
> 끝까지 추적하지 않은 것이 원인. 이후 복원에서는 모든 인터랙티브 요소에 대해 "누르면 무슨 일이 일어나는가"를 확인 대상으로 삼는다.
